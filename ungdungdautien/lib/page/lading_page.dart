import 'package:flutter/material.dart';

class Lading_page extends StatelessWidget {
  const Lading_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
